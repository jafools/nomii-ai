# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shenmay_ai

# Server
PORT=3001

# LLM (when ready)
CLAUDE_API_KEY=
LLM_PROVIDER=mock

# ── Playwright E2E ──────────────────────────────────────────────
# Used by tests/e2e/* + server/db/seed-test-admin.js. The seed is
# idempotent and safe to re-run, but it refuses to touch a
# DATABASE_URL that looks like prod (name not ending in _test /
# _staging / _dev / _ci).
TEST_ADMIN_EMAIL=e2e-admin@shenmay.test
TEST_ADMIN_PASSWORD=E2ETestPass!234
